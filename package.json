{
  "name": "sniper-terminal",
  "version": "1.0.0",
  "description": "S-Tier Crypto Trading Terminal",
  "main": "electron-main.js",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:start": "electron .",
    "electron:build": "npm run build && electron-builder",
    "mobile:init": "npm run build && npx cap init",
    "mobile:add-android": "npx cap add android",
    "mobile:build": "npm run build && npx cap copy && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "crypto-js": "^4.1.1",
    "lightweight-charts": "^4.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "electron": "^25.0.0",
    "electron-builder": "^24.0.0",
    "sharp": "^0.34.5",
    "typescript": "^5.0.0",
    "vite": "^4.0.0",
    "vite-plugin-pwa": "^1.2.0"
  },
  "build": {
    "appId": "com.sniper.terminal",
    "productName": "SniperTerminal",
    "mac": {
      "category": "public.app-category.finance"
    },
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "dist/**/*",
      "electron-main.js"
    ]
  }
}
